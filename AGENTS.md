# SkinCause 작업 안내

이 저장소에서 사람과 AI는 같은 제품 기준을 사용한다. 구현을 시작하기 전에 아래 순서로 맥락을 확인한다.

1. 담당 GitHub Story 또는 Task와 연결된 Sub-task
2. [통합 기능명세서](./docs/05-features/functional-specification.md)에서 담당 기능 ID를 눌러 연 상세 파일
3. [제품 규칙과 AI 경계](./docs/product/product-rules.md)
4. [요구사항 추적표](./docs/05-features/traceability.md)
5. [OpenAPI 계약](./docs/api/openapi.yaml)
6. [데이터 모델과 사전](./docs/06-data-model/README.md)

## 작업 원칙

- `Ready`인 Story나 Task의 Sub-task를 기준으로 작업한다.
- Sub-task의 결과·계약·예외·완료 조건은 지키되, 패키지·클래스·함수와 내부 아키텍처는 담당자가 현재 코드에 맞게 정한다.
- API가 바뀌면 OpenAPI와 관련 테스트를, 데이터 의미가 바뀌면 데이터 사전과 관련 테스트를 같은 PR에서 수정한다.
- 저장소는 단일 API 인스턴스가 사용하는 SQLite다. 스키마는 SQLite용 Flyway migration으로만 바꾸고 ORM 자동 생성에 맡기지 않는다.
- 모든 SQLite 연결은 FK 검사를 켜며, 쓰기 트랜잭션 안에서 AI·Object Storage 같은 외부 호출을 기다리지 않는다.
- 구현 중 제품 결정이 필요하면 추측해서 숨기지 말고 부모 Story에 결정이 필요한 이유와 선택지를 남긴다.
- 다른 사용자의 데이터 접근, AI 실패, 빈 상태와 재시도 경로를 정상 흐름과 함께 확인한다.
- 비밀값과 실제 사용자 기록을 저장소, 프롬프트, 로그나 테스트 fixture에 넣지 않는다.

## 바꾸면 안 되는 제품 경계

- 추천은 실제 카탈로그 안의 다음 실험 후보이며 피부 적합성이나 구매 성공 보장이 아니다.
- 피부 문제의 원인을 진단하거나 특정 제품을 범인으로 단정하지 않는다.
- 서버 규칙이 권한, 상태 전이, 비교 사실, 실제 제품 후보와 다음 실험 우선순위, Rescue 분류를 결정한다.
- AI는 이미지·자유 입력 구조화, 부족한 정보 질문과 계산된 결과 설명만 맡으며, 실패하거나 사용하지 않아도 고정 설명과 직접 입력으로 완료한다.
- 사용자가 요청하지 않은 메모·이미지 원문을 AI 공급자에게 보내지 않는다.
- 한 사용자는 한 번에 하나의 열린 실험만 가지며, 첫 사용 관찰 시각을 실제 실험 일정의 기준으로 삼는다.
- LAB 보상은 출석이 아니라 유효한 관찰과 실험 완료에 연결한다.

전체 문서의 역할과 읽는 순서는 [문서 안내](./docs/README.md)를 따른다.
