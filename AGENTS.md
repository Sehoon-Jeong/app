# SkinCause 작업 안내

구현 전에 아래 순서대로 읽는다.

1. 담당 GitHub Task와 부모 Feature 이슈 본문
2. Feature 이슈에서 연결한 [기능별 요구사항](./docs/04-requirements/README.md)
3. [제품 규칙과 AI 경계](./docs/product/product-rules.md)
4. [OpenAPI](./docs/api/openapi.yaml)
5. [데이터 모델](./docs/06-data-model/README.md)

GitHub Feature 이슈가 사용자 흐름·분기·실패·수용 조건의 유일한 기능명세다. 저장소에 별도 기능명세 문서를 만들거나 같은 내용을 복사하지 않는다.

## 작업 원칙

- `Ready`인 Task를 기준으로 구현한다. Feature는 사용자 결과, Task는 한 사람이 완료·검증할 수 있는 작업이다.
- Task의 결과, 예외와 완료 조건은 지키되 내부 클래스·함수·패키지는 현재 코드에 맞게 정한다.
- API가 바뀌면 OpenAPI와 계약 테스트를, 데이터 의미가 바뀌면 ERD·데이터 사전과 migration 테스트를 같은 PR에서 수정한다.
- SQLite 스키마는 Flyway migration으로만 바꾼다. 모든 연결에서 FK를 켠다.
- AI와 Object Storage 호출을 DB 쓰기 트랜잭션 안에서 기다리지 않는다.
- 다른 사용자의 데이터 접근, 빈 상태, AI 실패, 중복 제출과 재시도를 정상 흐름과 함께 테스트한다.
- 제품 결정이 필요하면 임의로 숨겨 정하지 말고 Feature에 배경, 선택지와 영향을 남긴다.
- 비밀값과 실제 사용자 기록을 저장소, 로그, 프롬프트 예제와 테스트 fixture에 넣지 않는다.

## 바꾸면 안 되는 경계

- 제품 의견과 추천은 구매 맥락의 설명이며 피부 적합성·안전·효능을 보장하지 않는다.
- Rescue는 확인 순위와 다음 루틴을 제안하지만 진단하거나 특정 제품을 원인으로 확정하지 않는다.
- AI는 자연어 이해, 허용된 웹 검색과 설명을 맡는다. 서버는 권한, 실제 제품 버전, 루틴 버전, 상태 전이와 AI 출력 참조를 검증한다.
- 새 루틴은 7일 뒤 실제 사용 일치와 전반적 결과를 묻는다. 7일은 의학 기준이 아니다.
- Rescue로 만든 루틴도 독립된 일반 루틴 버전이다.
- 제품 버전이 불확실하면 등록은 가능하지만 AI 판단 근거에서는 제외한다.

전체 문서의 역할은 [문서 안내](./docs/README.md)를 따른다.
